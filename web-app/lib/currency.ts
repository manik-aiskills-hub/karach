// Re-exports the shared implementation — see /app-logic/src/currency.ts for the
// actual logic and doc comments. Kept as a thin file so every existing
// "@/lib/currency" import in this app keeps working unchanged.
export * from "@kharch/app-logic/currency";
